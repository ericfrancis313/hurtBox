
<div class="box">
  <div class="text">
    <h1><%= @game.name %></h1>
    <h3>Select your fighter!</h3>
      <% @characters.each do |character| %>
        <div class='ryu'>
          <p class ="hyperText"> <%=  link_to character.name, character_path(character)%> </p>
        </div>
      <% end %>
  </div>
</div>
